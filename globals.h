#ifndef GLOBALS_H
#define GLOBALS_H

#include <stdio.h>

FILE *logfile;
FILE *seqlog;
FILE *terrain;
FILE *heightmap;
FILE *bpm;
FILE *rleOut;
FILE *rlelog;
// FILE *

bool hex_output = false;

#endif